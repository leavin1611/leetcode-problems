int searchInsert(int* nums, int numsSize, int target) {
    int index;
    for(int i=0;i<numsSize;i++)
    {
        if(nums[i]==target)
        {
         return i;
        }
        else if(target>nums[i])
        {
            index=i+1;
        }
       
        
    }

return index;
}
